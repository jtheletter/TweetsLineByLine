{
    "name": "twitterdialogbot",
    "version": "2.0.2",
    "description": "Twitter bot that posts written works line by line in order.",
    "main": "index.js",
    "scripts": {
        "test": "echo $1",
        "zip": "zip -r twitterdialogbot.zip . -x \"**/*.gif\" -x \"**/*.jp*\" -x \"**/*.mov\" -x \"**/*.mp*\" -x \"**/*.png\" -x \"**/.DS_Store\" -x \"**/__*\" -x \"*.git*\" -x \"*.md\" -x \"*.zip\" -x \".DS_Store\" -x \"__*\" -x \"check.js\" -x \"config/**/dev.js\" -x \"images*\"",
        "deploy": "aws lambda update-function-code --zip-file fileb://twitterdialogbot.zip --function-name"
    },
    "keywords": [],
    "author": "JP",
    "license": "ISC",
    "dependencies": {
        "twit": "^2.2.11"
    },
    "devDependencies": {},
    "repository": {
        "type": "git",
        "url": "git://github.com/jtheletter/twitterdialogbot.git"
    }
}
